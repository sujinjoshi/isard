const colors = require('./colors');

module.exports = {
  name: 'ISARD',
  short_name: 'ISARD',
  start_url: '/',
  background_color: colors.background,
  theme_color: colors.primary,
  display: 'standalone',
  icon: 'media/icon.png',
};
